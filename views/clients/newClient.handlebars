{{> header}}

<div class="container" id="projects">

	{{> side-nav}}

	<main>
		{{!-- <div class="dashboard-info">
			<p>gebruiker: {{req.session.username}}</p>
			<h1>{{title}}</h1>
			<a href="new">Nieuw project aanmaken</a>
		</div> --}}

		<form action="/clients/new" method="post">

			{{#if error}}<p class="error-log">{{error}}</p>{{/if}}


			<label for="client">Klant naam</label>
			<input id='clientName' name='clientName' type='text' value="{{clientName}}">


			<button type="submit">Aanmaken</button>
		</form>

	</main>
</div>
