{{> header}}

<div class="container" id="createAccount">

	{{> side-nav}}

	<main>
		<div class="icn-container1">
			{{#each data.permissions}}
				<div icn-user="{{name}}" class="icn-card1 n-{{level}}">
					<h2>{{name}}</h2>
					<h3>{{level}}</h3>
					<p>{{description}}</p>
				</div>
			{{/each}}
		</div>
	</main>
</div>
